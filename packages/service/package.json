{
  "name": "@caliper/service",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Application layer: ports, use-cases and the analysis pipeline. Runs unchanged in Node and in the browser.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@caliper/core": "*", "zod": "^3.24.1" }
}
